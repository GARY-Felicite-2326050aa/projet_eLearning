framework:
    ai:
        enabled: true
        chat:
            providers:
                anthropic:
                    api_key: '%env(ANTHROPIC_API_KEY)%'
                    model: 'claude-sonnet-4-20250514'
                    max_tokens: 4000
                    temperature: 0.7
                # openai:
                #     api_key: '%env(OPENAI_API_KEY)%'
                #     model: 'gpt-4'
